require('telescope').setup({})
-- telescope.load_extension('fzf')

-- telescope.setup({
--     pickers = {
--         find_files = {
--             find_command = {"rg", "--files", "-L"}
--         }
--     }
-- })
